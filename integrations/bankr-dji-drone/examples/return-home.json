{
  "prompt": "something looks wrong, bring it back",
  "steps": [
    { "endpoint": "POST /command", "body": { "action": "return_to_home" }, "expect": { "ok": true, "state": { "flight_status": "returning" } } }
  ],
  "note": "return_to_home is the universal safe fallback — always allowed, never refused.",
  "expectReport": "🚁 returning to home and landing on the dock — battery 71%."
}
