import B20SkillDemoClient from "./B20SkillDemoClient";

export const metadata = {
  title: "B20 Proof Gateway | AI2Human",
  description:
    "B20 handles native token rules. AI2Human handles who is qualified to receive roles, mint access, and allowlist permissions."
};

export default function B20SkillPage() {
  return <B20SkillDemoClient />;
}
