import styles from "../market.module.css";

export default function ProtocolPage() {
  return (
    <section className={styles.placeholderCard}>
      <h1>Protocol</h1>
      <p>Protocol docs are not part of this clone scope. The requested work was the agents, tasks, and create-task pages.</p>
    </section>
  );
}
